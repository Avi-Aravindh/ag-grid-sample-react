import { createMuiTheme } from '@material-ui/core/styles';

export const AppTheme = createMuiTheme({
  palette: {
    primary: {
      main: '#3B5096',
    },
    typography: {
      fontFamily: ['Poppins'],
    },
  },
});
